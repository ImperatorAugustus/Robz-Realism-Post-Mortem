{selector
	{type "searchlight"}

	{group
		{sort {class tank}}
		{sort {class light_tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		{sort {class cannon}}
		{sort {class human} {weapon}}
		{sort {class human}}
		{sort time_to_aim}
	}

	{group
		{sort distance}
	}
}